# Gradle
.gradle/
build/

# OpenAPI 生成コード（build/ の外・プロジェクト直下に出すが、生成物なので追跡しない）
generated/

# Kotlin 拡張(fwcd)が生成するコンパイル出力。ビルド生成物なので追跡しない。
bin/

# mise が DL したツール類は含めない（mise.toml で再現できる）

# 環境変数ファイル（現在は未使用だが、誤って秘密情報入りを作ってもコミットしないよう保険）
.env

# IDE
.idea/
*.iml
.kotlin/

# OS
.DS_Store

# ログ
*.log
